# import
#     sdl2,
#     random,
#     math 

# import
#     rectangle,
#     matrix

# type Level = object
#     walls: Matrix[int]
#     textures: Matrix[sdl2.Rect]

# proc layoutRooms(sizeAvg, sizeStdv: float,
#                  numRooms, width, height: int, rng: Rand) =
#     let mut

# proc generateDungeon(width, height: int, rng: Rand): Level =
#     let walls = newMatrix(width, height)

#     let rooms = layoutRooms(5, 1, 12)